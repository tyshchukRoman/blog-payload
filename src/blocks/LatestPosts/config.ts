import type { Block } from 'payload'

export const LatestPosts: Block = {
  slug: 'latestPosts',
  fields: [],
  interfaceName: 'Latest Posts',
  labels: {
    plural: 'Latest Posts',
    singular: 'Latest Posts',
  },
}
